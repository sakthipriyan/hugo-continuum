---
title: "workshop_tags"
# Hugo generates a list page for every taxonomy. Continuum permalinks the
# browsable tag pages under /<section>/tags/, so this default one is an empty
# duplicate -- suppress it rather than publishing an empty page and feed.
build:
  render: never
  list: never
---
